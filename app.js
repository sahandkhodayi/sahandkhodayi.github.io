const { SITE, PROJECTS, EXPERIMENTS, KNOWLEDGE, CONNECTIONS, SYSTEMS_TOPICS, MEM } = window;
const h = value => String(value).replace(/&/g,"&amp;").replace(/</g,"&lt;").replace(/>/g,"&gt;");
const rows = data => `<dl class="spec">${data.map(([k,v])=>`<div class="spec-row"><dt>${h(k)}</dt><dd>${h(v)}</dd></div>`).join("")}</dl>`;
const tags = list => `<div class="entry-tags">${list.map(x=>`<span class="tag">${h(x)}</span>`).join("")}</div>`;
function sectionHead(title,note){return `<div class="sec-h"><h2>${h(title)}</h2><span class="line"></span><span class="n">${h(note)}</span></div>`}
function projectEntry(p){return `<article class="entry"><div class="entry-id">[${p.id}]</div><div><h3 class="entry-title"><a href="#/projects/${p.slug}">${h(p.title)}</a></h3><p class="entry-desc">${h(p.blurb)}</p>${tags(p.tags)}</div><div class="entry-side"><span class="tag ${p.status==='active'?'a':''}">${h(p.status)}</span></div></article>`}
function Home(){return `<div class="view"><section class="hero"><div class="container"><div class="kicker">// larpsociety / index</div><h1>I build things to understand how they work<span class="cur"></span></h1><p class="lede">AI systems, mathematics, and the layers underneath computers — from neural networks and tensors down to C, memory, and the machine that executes them.</p><div class="hero-meta"><span class="hl">SAHAND KHODAYI</span><span class="sep">·</span><span>AI / ML</span><span class="sep">·</span><span>MATHEMATICS</span><span class="sep">·</span><span>C / SYSTEMS</span></div></div></section><section class="sec"><div class="container">${sectionHead('direction','how the layers connect')}<ol class="chain"><li><span class="t">mathematics</span><span class="d">linear algebra · calculus · probability · optimization</span></li><li><span class="t">neural networks</span><span class="d">convolution · activations · backpropagation</span></li><li><span class="t">pytorch</span><span class="d">tensors · datasets · training loops</span></li><li><span class="t">c inference</span><span class="d">pointers · memory layout · manual forward pass</span></li><li><span class="t">the machine</span><span class="d">bytes · cache · instructions · systems</span></li></ol></div></section><section class="sec"><div class="container">${sectionHead('selected work',String(PROJECTS.length).padStart(2,'0')+' entries')}${PROJECTS.map(projectEntry).join('')}<div class="linkrow"><a class="btn solid" href="#/projects">all projects →</a><a class="btn" href="#/lab">open lab →</a></div></div></section><section class="sec"><div class="container">${sectionHead('knowledge','engineering reference')}<p class="lede dim mb">Notes are the memory of the work: concepts, mathematics, implementation details, and the mistakes worth remembering.</p><div class="tree">${KNOWLEDGE.map(b=>`<details><summary>${h(b.branch)}<span class="cnt">${b.leaves.length} notes</span></summary><div class="leaves">${b.leaves.map(x=>`<div class="leaf"><span>${h(x.name)}</span><span class="m">${h(x.meta)}</span><span class="st ${x.st}">${x.st==='built'?'applied':'studying'}</span></div>`).join('')}</div></details>`).join('')}</div><div class="linkrow"><a class="btn" href="#/knowledge">enter knowledge base →</a></div></div></section><section class="sec"><div class="container">${sectionHead('lab','questions → experiments → findings')}${EXPERIMENTS.slice(0,4).map(e=>`<div class="exp"><div style="display:grid;grid-template-columns:52px 1fr auto;gap:20px;padding:15px 0;align-items:baseline"><span class="exp-id">[${e.id}]</span><span class="exp-title">${h(e.title)}</span><span class="tag ${e.tag}">${h(e.status)}</span></div></div>`).join('')}<div class="linkrow"><a class="btn" href="#/lab">full lab log →</a></div></div></section></div>`}
function Projects(){return `<div class="view"><section class="sec container">${sectionHead('projects','built things')}<p class="lede dim mb">Projects are presented as technical systems: what goes in, what happens, what comes out, and which abstractions were removed.</p>${PROJECTS.map(projectEntry).join('')}</section></div>`}
function ProjectDetail(){const slug=currentPath().split('/')[2],p=PROJECTS.find(x=>x.slug===slug);if(!p)return NotFound();const ex=EXPERIMENTS.filter(e=>p.related.includes(e.id));return `<div class="view"><section class="sec container"><div class="kicker"><a href="#/projects" style="border:0">projects</a> / ${h(p.slug)}</div><h1 style="font-size:clamp(22px,4vw,34px);margin-top:16px">${h(p.title)}</h1><p class="lede" style="margin-top:14px">${h(p.blurb)}</p>${tags(p.tags)}</section><section class="sec"><div class="container">${sectionHead('specification','['+p.id+']')}<div class="panel"><div class="panel-h">system profile <span class="r">LARPSOCIETY</span></div><div class="panel-b">${rows(p.spec)}</div></div></div></section><section class="sec"><div class="container">${sectionHead('pipeline','flow')}<ol class="chain">${p.pipeline.map(([a,b])=>`<li><span class="t">${h(a)}</span><span class="d">${h(b)}</span></li>`).join('')}</ol></div></section><section class="sec"><div class="container">${sectionHead('questions','what this project tests')}<div class="panel"><div class="panel-b" style="padding:0">${p.questions.map((q,i)=>`<div style="display:grid;grid-template-columns:42px 1fr;padding:14px 15px;border-bottom:1px solid var(--line)"><span style="color:var(--fg-4);font-size:10px">Q${i+1}</span><span style="color:var(--fg-2);font-size:12px">${h(q)}</span></div>`).join('')}</div></div></div></section>${ex.length?`<section class="sec"><div class="container">${sectionHead('related','lab')}<div class="panel"><div class="panel-b">${ex.map(e=>`<div style="display:flex;gap:13px;padding:7px 0;align-items:baseline"><span class="exp-id">[${e.id}]</span><span class="exp-title">${h(e.title)}</span><span class="tag ${e.tag}" style="margin-left:auto">${h(e.status)}</span></div>`).join('')}</div></div></div></section>`:''}<section class="sec"><div class="container"><div class="linkrow"><a class="btn solid" href="https://${p.spec.find(x=>x[0]==='source')?.[1]||''}" target="_blank" rel="noopener">source ↗</a><a class="btn" href="#/knowledge">knowledge →</a><a class="btn" href="#/lab">lab →</a></div></div></section></div>`}
function Lab(){return `<div class="view"><section class="sec container">${sectionHead('lab',EXPERIMENTS.length+' entries')}<p class="lede dim mb">Not a changelog. Each entry is a question worth answering. Planned means planned. Built means I actually ran it.</p>${EXPERIMENTS.map(e=>`<details class="exp"><summary><span class="exp-id">[${e.id}]</span><span class="exp-title">${h(e.title)}</span><span class="tag ${e.tag}">${h(e.status)}</span></summary><div class="exp-body"><p>${h(e.body)}</p>${rows(e.spec)}</div></details>`).join('')}</section></div>`}
function Knowledge(){return `<div class="view"><section class="sec container">${sectionHead('knowledge','selected branches')}<p class="lede dim mb">A public slice of the engineering notebook. Concepts stay connected to projects and experiments instead of becoming disconnected definitions.</p><div class="tree">${KNOWLEDGE.map(b=>`<details open><summary>${h(b.branch)}<span class="cnt">${b.leaves.length} notes</span></summary><div class="leaves">${b.leaves.map(x=>`<div class="leaf"><span>${h(x.name)}</span><span class="m">${h(x.meta)}</span><span class="st ${x.st}">${x.st==='built'?'applied':'studying'}</span></div>`).join('')}</div></details>`).join('')}</div></section><section class="sec"><div class="container">${sectionHead('connections','knowledge ↔ work')}<div class="panel"><div class="panel-b" style="padding:0">${CONNECTIONS.map(([a,b,c])=>`<div style="display:grid;grid-template-columns:1fr auto 1fr auto 1fr;gap:9px;padding:12px 15px;border-bottom:1px solid var(--line);font-size:11.5px"><span>${h(a)}</span><span class="exp-id">→</span><span style="color:var(--fg-2)">${h(b)}</span><span class="exp-id">→</span><span class="exp-id">${h(c)}</span></div>`).join('')}</div></div></div></section></div>`}
function Systems(){return `<div class="view"><section class="sec container">${sectionHead('systems','below the abstraction')}<p class="lede dim mb">The computer is not a black box. This is the layer underneath Python and ML frameworks.</p><div class="panel"><div class="panel-h">topics</div><div class="panel-b">${rows(SYSTEMS_TOPICS)}</div></div></section><section class="sec"><div class="container">${sectionHead('memory','interactive')}<p class="lede dim mb">A small byte buffer. Click a cell and watch the pointer move.</p><div class="panel"><div class="panel-h">uint8_t buf[16] <span class="r">stack</span></div><div class="panel-b"><div class="memgrid" id="memgrid">${MEM.bytes.map((b,i)=>`<div class="cell" data-i="${i}"><span class="a">+${i.toString(16).padStart(2,'0')}</span>${b.toString(16).padStart(2,'0')}</div>`).join('')}</div><div class="readout"><div><div class="k">pointer</div><div class="v acc" id="ptr-addr">—</div></div><div><div class="k">*pointer</div><div class="v" id="ptr-val">—</div></div><div><div class="k">offset</div><div class="v" id="ptr-off">—</div></div></div></div></div></div></section><section class="sec"><div class="container">${sectionHead('address space','process')}<div class="panel"><div class="panel-b"><pre class="ascii"><span class="d">high addresses</span>\n<span class="c">┌───────────────────────────┐</span>\n<span class="c">│</span> stack          <span class="d">↓</span> locals / frames <span class="c">│</span>\n<span class="c">├───────────────────────────┤</span>\n<span class="c">│</span>             unused        <span class="c">│</span>\n<span class="c">├───────────────────────────┤</span>\n<span class="c">│</span> heap           <span class="d">↑</span> malloc    <span class="c">│</span>\n<span class="c">├───────────────────────────┤</span>\n<span class="c">│</span> data / bss                 <span class="c">│</span>\n<span class="c">├───────────────────────────┤</span>\n<span class="c">│</span> text / instructions        <span class="c">│</span>\n<span class="c">└───────────────────────────┘</span>\n<span class="d">low addresses</span></pre></div></div></div></section></div>`}
function About(){return `<div class="view"><section class="sec container">${sectionHead('about','larpsociety')}<p class="lede">${h(SITE.name)} — ${h(SITE.identity)}.</p><p class="lede dim" style="margin-top:18px;max-width:70ch">I'm interested in the full path from mathematical idea to running program. I use Python and PyTorch to build ML systems, C to remove abstractions when useful, and mathematics to understand why systems behave the way they do.</p></section><section class="sec"><div class="container">${sectionHead('manifesto','working principles')}<ol class="chain"><li><span class="t">don't just use it</span><span class="d">understand the mechanism behind the abstraction</span></li><li><span class="t">measure it</span><span class="d">replace assumptions with experiments</span></li><li><span class="t">build it twice</span><span class="d">high-level implementation → lower-level implementation</span></li><li><span class="t">document the failure</span><span class="d">mistakes are part of the system's history</span></li></ol></div></section><section class="sec"><div class="container">${sectionHead('contact','public endpoints')}<div class="panel"><div class="panel-b">${rows([["github",SITE.github.replace('https://','')],["email",SITE.email],["site","LARPSOCIETY / GitHub Pages"]])}</div></div></div></section></div>`}
function NotFound(){return `<div class="view"><section class="sec container"><div class="kicker">// 404</div><h1 style="font-size:24px;margin-top:16px">route not found</h1><p class="lede dim" style="margin-top:12px">The machine looked. Nothing answered.</p><div class="linkrow"><a class="btn solid" href="#/">← return to index</a></div></section></div>`}
const ROUTES={'/':Home,'/projects':Projects,'/lab':Lab,'/knowledge':Knowledge,'/systems':Systems,'/about':About};
const app=document.getElementById('app');
function currentPath(){const p=location.hash.replace(/^#/,'');if(!p||p==='/')return '/';return p.replace(/\/+$/,'')||'/'}
function mount(){const grid=document.getElementById('memgrid');if(!grid)return;const cells=[...grid.querySelectorAll('.cell')];let ptr=0;const paint=()=>{cells.forEach((c,i)=>c.classList.toggle('ptr',i===ptr));document.getElementById('ptr-addr').textContent='0x'+(MEM.base+ptr).toString(16);document.getElementById('ptr-val').textContent='0x'+MEM.bytes[ptr].toString(16).padStart(2,'0');document.getElementById('ptr-off').textContent='+'+ptr};cells.forEach((c,i)=>c.onclick=()=>{ptr=i;paint()});paint()}
function render(){const path=currentPath();const view=path.startsWith('/projects/')?ProjectDetail:ROUTES[path]||NotFound;app.innerHTML=view();document.querySelectorAll('.nav a').forEach(a=>{const t=a.getAttribute('href').replace(/^#/,'');a.classList.toggle('on',t==='/'?path==='/':path===t||path.startsWith(t+'/'))});window.scrollTo(0,0);mount()}
window.addEventListener('hashchange',render);render();
